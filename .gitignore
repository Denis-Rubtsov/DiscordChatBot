bin/
obj/
/packages/
riderModule.iml
/_ReSharper.Caches/
.idea/
.DS_Store
.claude/

# Secrets - real values are injected via environment variables, never committed
DiscordChatBot/appsettings.Production.json
DiscordChatBot/appsettings.Local.json
.env

# Runtime data (persisted relationship memory), not repo content
DiscordChatBot/relationships.json
DiscordChatBot/botstate.json

# VPS publish output (real secrets live in out/appsettings.json there, never committed)
out/